import { ImgScaleDirective } from './img-scale.directive';

describe('ImgScaleDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgScaleDirective();
    expect(directive).toBeTruthy();
  });
});

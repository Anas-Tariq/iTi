import { OnHoverChangeDirective } from './on-hover-change.directive';

describe('OnHoverChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new OnHoverChangeDirective();
    expect(directive).toBeTruthy();
  });
});
